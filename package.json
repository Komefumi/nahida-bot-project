{
  "name": "an-nahda-server-bot",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start:prod": "npx build && node build/index.js",
    "start:dev": "npx nodemon",
    "build": "rimraf ./build && tsc",
    "gen:translation-base": "ts-node scripts/generate-translation-base.ts"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.8.3",
    "eslint": "^8.51.0",
    "nodemon": "^3.0.1",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@prisma/client": "5.4.2",
    "discord.js": "^14.13.0",
    "dotenv": "^16.3.1",
    "prisma": "^5.4.2"
  }
}
